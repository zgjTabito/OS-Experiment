# 操作系统第八次实验

杨博涵 张耕嘉 刘嘉昊

### 练习0：填写已有实验

>本实验依赖实验2/3/4/5/6/7。请把你做的实验2/3/4/5/6/7的代码填入本实验中代码中有“LAB2”/“LAB3”/“LAB4”/“LAB5”/“LAB6” /“LAB7”的注释相应部分。并确保编译通过。注意：为了能够正确执行lab8的测试应用程序，可能需对已完成的实验2/3/4/5/6/7的代码进行进一步改进。

对代码中有LAB注释的地方进行填入，这里只展示为LAB8做相关改进的部分。

```C
void proc_run(struct proc_struct *proc)
{
    if (proc != current)
    {
        bool intr_flag;
        struct proc_struct *prev = current;
        
        // 1. 禁用中断
        local_intr_save(intr_flag);
        
        // 2. 更新当前进程
        current = proc;
        
        // 3. 切换页表到新进程的地址空间
        lsatp(proc->pgdir);

        // 3.5. 刷新 TLB，避免旧进程残留的映射影响新进程
        flush_tlb();
        
        // 4. 执行上下文切换
        switch_to(&(prev->context), &(proc->context));
        
        // 5. 恢复中断（在 switch_to 返回后执行）
        local_intr_restore(intr_flag);
    }
    
}
```

`proc_run` 在切换进程时新增 `flush_tlb()`。lab8 引入每进程独立页表和文件系统后，进程切换时要先切 SATP（`lsatp(proc->pgdir)`），再刷新 TLB，避免前一个进程残留的地址映射被误用，保证新进程的虚拟内存映射一致、页缓存不会脏读旧映射。

```c
static struct proc_struct *
alloc_proc(void)
{
    struct proc_struct *proc = kmalloc(sizeof(struct proc_struct));
    if (proc != NULL)
    {
        proc->state = PROC_UNINIT;
        proc->pid = -1;
        proc->runs = 0;
        proc->kstack = 0;
        proc->need_resched = 0;
        proc->parent = NULL;
        proc->mm = NULL;
        memset(&(proc->context), 0, sizeof(struct context));
        proc->tf = NULL;
        proc->pgdir = boot_pgdir_pa;
        proc->flags = 0;
        memset(proc->name, 0, PROC_NAME_LEN);
        // lab5 add:
        proc->wait_state = 0;
        proc->cptr = proc->optr = proc->yptr = NULL;
        proc->rq = NULL;              // 初始化运行队列为空
        list_init(&(proc->run_link)); // 初始化运行队列的指针
        proc->time_slice = 0;
        proc->lab6_run_pool.left = proc->lab6_run_pool.right = proc->lab6_run_pool.parent = NULL;
        proc->lab6_stride = 0;
        proc->lab6_priority = 0;
        proc->filesp = NULL;

        
    }
    return proc;
}
```

添加初始化 proc->filesp = NULL：lab8 引入文件系统层，proc_struct 新增 filesp 指向该进程的文件描述符表。alloc_proc 里把它置空，表示初始还未绑定文件表；后续在 proc_init 为 idleproc 创建空表，在 copy_files()/put_files() 中按 fork/exit 生命周期复制或递减引用。

## 练习一：完成读文件操作的实现（需要编码）

> 首先了解打开文件的处理流程，然后参考本实验后续的文件读写操作的过程分析，填写在 kern/fs/sfs/sfs_inode.c中 的sfs_io_nolock()函数，实现读文件中数据的代码。

完成了 `sfs_io_nolock()` 的核心读写逻辑，遵循“首尾块按偏移处理 + 中间整块直传”的三段式流程。

```C
    blkoff = offset % SFS_BLKSIZE;
    if (blkoff != 0) {
        size = (nblks != 0) ? (SFS_BLKSIZE - blkoff) : (endpos - offset);
        if ((ret = sfs_bmap_load_nolock(sfs, sin, blkno, &ino)) != 0) {
            goto out;
        }
        if ((ret = sfs_buf_op(sfs, data, size, ino, blkoff)) != 0) {
            goto out;
        }
        data += size;
        alen += size;
        offset += size;
        blkno ++;
        if (nblks != 0) {
            nblks --;
        }
    }

    /*
     * (2) Transfer the fully aligned blocks in the middle.
     */
    while (nblks != 0) {
        if ((ret = sfs_bmap_load_nolock(sfs, sin, blkno, &ino)) != 0) {
            goto out;
        }
        size = SFS_BLKSIZE;
        if ((ret = sfs_block_op(sfs, data, ino, 1)) != 0) {
            goto out;
        }
        data += size;
        alen += size;
        offset += size;
        blkno ++;
        nblks --;
    }

    /*
     * (3) Handle the trailing partial block if end position is not aligned.
     */
    if (offset < endpos) {
        size = endpos - offset;
        if ((ret = sfs_bmap_load_nolock(sfs, sin, blkno, &ino)) != 0) {
            goto out;
        }
        if ((ret = sfs_buf_op(sfs, data, size, ino, 0)) != 0) {
            goto out;
        }
        alen += size;
    }
```

- **首个非对齐块**：计算 `blkoff = offset % SFS_BLKSIZE`，若不为 0，先通过 `sfs_bmap_load_nolock` 找到对应物理块号 `ino`，用 `sfs_buf_op` 从偏移 `blkoff` 处读/写 `size` 字节（剩余或全量），再推进指针与计数。
- **中间整块**：循环 `nblks`，每次 `sfs_bmap_load_nolock` 获取块号，再用 `sfs_block_op` 直接整块传输（块大小 `SFS_BLKSIZE`），更新数据指针与累计长度。
- **末尾非对齐块**：若 `offset < endpos`，再次映射块号后调用 `sfs_buf_op` 处理末尾残留字节，完成对齐之外的收尾操作。
- 读写均复用同一逻辑，借助函数指针 `sfs_buf_op`/`sfs_block_op` 在读写模式间切换；结束时把实际传输长度写回 `alen`，并在写入路径下更新文件大小与脏标记。
- 错误处理：任一步 `bmap` 或块操作出错即跳转 `out` 返回错误，已传输长度会通过 `*alenp` 反馈给上层，保证部分成功时可见。

## 练习2: 完成基于文件系统的执行程序机制的实现（需要编码）

>改写proc.c中的load_icode函数和其他相关函数，实现基于文件系统的执行程序机制。执行：make qemu。如果能看看到sh用户程序的执行界面，则基本成功了。如果在sh用户界面上可以执行`exit`, `hello`（更多用户程序放在`user`目录下）等其他放置在`sfs`文件系统中的其他执行程序，则可以认为本实验基本成功。

load_icode：

```c
static int
load_icode(int fd, int argc, char **kargv)
{
    if (current->mm != NULL)
    {
        panic("load_icode: current->mm must be empty.\n");
    }

    int ret = -E_NO_MEM;
    struct mm_struct *mm;
    struct elfhdr elf;
    struct proghdr *ph = NULL;

    /* (1) create a new mm for current process */
    if ((mm = mm_create()) == NULL)
    {
        goto bad_mm;
    }

    /* (2) create a new PDT, and mm->pgdir = kernel virtual addr of PDT */
    if ((ret = setup_pgdir(mm)) != 0)
    {
        goto bad_pgdir_cleanup_mm;
    }

    /* (3.1) read ELF header */
    if ((ret = load_icode_read(fd, &elf, sizeof(struct elfhdr), 0)) != 0)
    {
        goto bad_elf_cleanup_pgdir;
    }
    if (elf.e_magic != ELF_MAGIC)
    {
        ret = -E_INVAL_ELF;
        goto bad_elf_cleanup_pgdir;
    }

    /* (3.2) read program headers */
    size_t phdroff = elf.e_phoff, phnum = elf.e_phnum;
    size_t phentsize = elf.e_phentsize;
    size_t phsize = phnum * phentsize;
    if ((ph = kmalloc(phsize)) == NULL)
    {
        goto bad_elf_cleanup_pgdir;
    }
    if ((ret = load_icode_read(fd, ph, phsize, phdroff)) != 0)
    {
        goto bad_cleanup_ph;
    }

    struct Page *page = NULL;
    uint32_t vm_flags, perm;
    struct proghdr *phe = (struct proghdr *)((char *)ph + phsize);
    struct proghdr *p;

    for (p = ph; p < phe; p++)
    {
        /* (3.4) only load segments */
        if (p->p_type != ELF_PT_LOAD)
        {
            continue;
        }
        if (p->p_filesz > p->p_memsz)
        {
            ret = -E_INVAL_ELF;
            goto bad_cleanup_mmap;
        }

        /* (3.5) setup vma for this segment */
        vm_flags = 0;
        perm = PTE_U | PTE_V;
        if (p->p_flags & ELF_PF_X)
            vm_flags |= VM_EXEC;
        if (p->p_flags & ELF_PF_W)
            vm_flags |= VM_WRITE;
        if (p->p_flags & ELF_PF_R)
            vm_flags |= VM_READ;

        if (vm_flags & VM_READ)
            perm |= PTE_R;
        if (vm_flags & VM_WRITE)
            perm |= (PTE_W | PTE_R);
        if (vm_flags & VM_EXEC)
            perm |= PTE_X;

        if ((ret = mm_map(mm, p->p_va, p->p_memsz, vm_flags, NULL)) != 0)
        {
            goto bad_cleanup_mmap;
        }

        /* (3.6.1) load file-backed part */
        uintptr_t start = p->p_va, la = ROUNDDOWN(start, PGSIZE);
        uintptr_t end = p->p_va + p->p_filesz;
        size_t off, size;

        while (start < end)
        {
            if ((page = pgdir_alloc_page(mm->pgdir, la, perm)) == NULL)
            {
                goto bad_cleanup_mmap;
            }
            off = start - la;
            size = PGSIZE - off;
            if (end < la + PGSIZE)
            {
                size -= la + PGSIZE - end;
            }
            if ((ret = load_icode_read(fd, page2kva(page) + off, size, p->p_offset + (start - p->p_va))) != 0)
            {
                goto bad_cleanup_mmap;
            }
            start += size;
            la += PGSIZE;
        }

        /* (3.6.2) build BSS part */
        end = p->p_va + p->p_memsz;
        if (start < la)
        {
            if (start == end)
            {
                continue;
            }
            off = start - (la - PGSIZE);
            size = PGSIZE - off;
            if (end < la)
            {
                size -= la - end;
            }
            memset(page2kva(page) + off, 0, size);
            start += size;
        }
        while (start < end)
        {
            if ((page = pgdir_alloc_page(mm->pgdir, la, perm)) == NULL)
            {
                goto bad_cleanup_mmap;
            }
            off = start - la;
            size = PGSIZE - off;
            if (end < la + PGSIZE)
            {
                size -= la + PGSIZE - end;
            }
            memset(page2kva(page) + off, 0, size);
            start += size;
            la += PGSIZE;
        }
    }

    /* (4) build user stack memory */
    vm_flags = VM_READ | VM_WRITE | VM_STACK;
    if ((ret = mm_map(mm, USTACKTOP - USTACKSIZE, USTACKSIZE, vm_flags, NULL)) != 0)
    {
        goto bad_cleanup_mmap;
    }
    assert(pgdir_alloc_page(mm->pgdir, USTACKTOP - PGSIZE, PTE_USER) != NULL);
    assert(pgdir_alloc_page(mm->pgdir, USTACKTOP - 2 * PGSIZE, PTE_USER) != NULL);
    assert(pgdir_alloc_page(mm->pgdir, USTACKTOP - 3 * PGSIZE, PTE_USER) != NULL);
    assert(pgdir_alloc_page(mm->pgdir, USTACKTOP - 4 * PGSIZE, PTE_USER) != NULL);

    /* (5) install mm and switch page table early so copy_to_user works */
    mm_count_inc(mm);
    current->mm = mm;
    current->pgdir = PADDR(mm->pgdir);
    lsatp(PADDR(mm->pgdir));

    /* (6) setup argc/argv in user stack */
    uintptr_t stacktop = USTACKTOP;
    uintptr_t uargv[EXEC_MAX_ARG_NUM];
    int i;
    for (i = argc - 1; i >= 0; i--)
    {
        size_t len = strlen(kargv[i]) + 1;
        stacktop -= len;
        if (!copy_to_user(mm, (void *)stacktop, kargv[i], len))
        {
            ret = -E_INVAL;
            goto bad_cleanup_mmap;
        }
        uargv[i] = stacktop;
    }
    stacktop = ROUNDDOWN(stacktop, sizeof(uintptr_t));
    stacktop -= (argc + 1) * sizeof(uintptr_t);
    uintptr_t argv_base = stacktop;
    if (!copy_to_user(mm, (void *)stacktop, uargv, argc * sizeof(uintptr_t)))
    {
        ret = -E_INVAL;
        goto bad_cleanup_mmap;
    }
    uintptr_t nullp = 0;
    if (!copy_to_user(mm, (void *)(stacktop + argc * sizeof(uintptr_t)), &nullp, sizeof(uintptr_t)))
    {
        ret = -E_INVAL;
        goto bad_cleanup_mmap;
    }
    /* push argc on stack so sp points to argc, argv array follows */
    stacktop -= sizeof(uintptr_t);
    uintptr_t cargc = (uintptr_t)argc;
    if (!copy_to_user(mm, (void *)stacktop, &cargc, sizeof(uintptr_t)))
    {
        ret = -E_INVAL;
        goto bad_cleanup_mmap;
    }

    /* (7) setup trapframe for user environment */
    struct trapframe *tf = current->tf;
    uintptr_t sstatus = tf->status;
    memset(tf, 0, sizeof(struct trapframe));
    tf->gpr.sp = stacktop;
    tf->epc = elf.e_entry;
    tf->status = (sstatus | SSTATUS_SPIE) & ~SSTATUS_SPP & ~SSTATUS_SIE;
    tf->gpr.a0 = argc;
    tf->gpr.a1 = argv_base;

    ret = 0;
    goto out;

bad_cleanup_mmap:
    if (current->mm == mm)
    {
        lsatp(boot_pgdir_pa);
        current->mm = NULL;
        mm_count_dec(mm);
    }
    exit_mmap(mm);
bad_cleanup_ph:
    if (ph != NULL)
    {
        kfree(ph);
        ph = NULL;
    }
bad_elf_cleanup_pgdir:
    put_pgdir(mm);
bad_pgdir_cleanup_mm:
    mm_destroy(mm);
bad_mm:
out:
    if (ph != NULL)
    {
        kfree(ph);
    }
    sysfile_close(fd);
    return ret;
}
```

以下为 LAB8 练习 2 实验报告要点（基于文件系统的执行程序机制实现），可直接整理进你的报告。

1) 文件读取链路  
   - 新增 `load_icode_read(fd, buf, len, offset)` 封装 `sysfile_seek`/`sysfile_read`，按偏移读取 ELF 头、程序头及各段数据。  
   - `load_icode` 首先读取 ELF 头并校验魔数，再 kmalloc 程序头数组并读入。  
2) 段映射与加载  
   - 仍对每个 `PT_LOAD` 段创建 VMA（`mm_map`）并计算权限位 `PTE_{R,W,X,U,V}`。  
   - 文件内容部分：逐页 `pgdir_alloc_page` 后调用 `load_icode_read` 把文件数据搬到物理页。  
   - BSS 部分：对文件尾到 memsz 的区域按页 `memset 0`。  
3) 用户栈与参数布局  
   - 先 `mm_map` 用户栈并分配多页。  
   - 提前安装新页表：`mm_count_inc`、`current->mm/pgdir`、`lsatp`，以便 `copy_to_user` 可用。  
   - 从栈顶向下拷贝每个字符串，再放置 argv 指针数组（结尾 NULL），最后压入 argc，使用户态入口看到 `a0=argc`、`a1=argv_base`。  
4) trapframe 设置  
   - 清零 tf，设置 `sp=stacktop`、`epc=elf.e_entry`，`status` 置位 `SPIE` 并清 `SPP/SIE`，寄存器 `a0/a1` 传递参数。  
5) 资源与错误处理  
   - 若已切换到新 mm 失败，恢复 `satp` 到 `boot_pgdir_pa`，`mm_count_dec`，并 `exit_mmap/put_pgdir/mm_destroy`。  
   - 释放 `kmalloc` 的程序头缓冲、关闭 fd。  
6) `do_execve` 侧的适配  
   - 校验 `argc` 范围，使用 `copy_kargv` 把用户 argv 拷到内核缓冲；旧 mm 释放后调用 `load_icode(fd, argc, kargv)`；最后 `set_proc_name`。

```c
int do_fork(uint32_t clone_flags, uintptr_t stack, struct trapframe *tf)
{
    int ret = -E_NO_FREE_PROC;
    struct proc_struct *proc;
    if (nr_process >= MAX_PROCESS)
    {
        goto fork_out;
    }
    ret = -E_NO_MEM;

    if ((proc = alloc_proc()) == NULL)
    {
        goto fork_out;
    }

    if (setup_kstack(proc) != 0)
    {
        goto bad_fork_cleanup_proc;
    }

    if (copy_mm(clone_flags, proc) != 0)
    {
        goto bad_fork_cleanup_kstack;
    }

    if ((ret = copy_files(clone_flags, proc)) != 0)
    { // for LAB8
        goto bad_fork_cleanup_mm;
    }

    copy_thread(proc, stack, tf);

    bool intr_flag;
    local_intr_save(intr_flag);
    {
        proc->pid = get_pid();
        hash_proc(proc);

        // 只通过 set_links 把新进程挂到 proc_list，并维护关系/计数
        proc->parent = current;
        current->wait_state = 0;
        set_links(proc);
    }
    local_intr_restore(intr_flag);

    wakeup_proc(proc);

    ret = proc->pid;
    
fork_out:
    return ret;

bad_fork_cleanup_mm:
    if (proc->mm != NULL)
    {
        if (mm_count_dec(proc->mm) == 0)
        {
            exit_mmap(proc->mm);
            put_pgdir(proc->mm);
            mm_destroy(proc->mm);
        }
        proc->mm = NULL;
    }
bad_fork_cleanup_kstack:
    put_kstack(proc);
bad_fork_cleanup_proc:
    kfree(proc);
    goto fork_out;
}
```

- 新增文件描述符复制：在复制地址空间后调用 `copy_files(clone_flags, proc)`，支持文件系统语义的继承/共享；失败时跳转到文件专用清理路径。
- 资源回滚完善：新增 `bad_fork_cleanup_mm` 分支，若 mm 引用计数减为 0 则执行 `exit_mmap/put_pgdir/mm_destroy`，并在下游统一释放内核栈/pcb，避免文件复制失败时泄漏。
- 进程关系与等待状态：在持锁区显式设 `proc->parent = current` 且将 `current->wait_state` 清 0，再通过 `set_links` 挂接到全局进程链表，保证 wait/exit 语义正确 。
- 结构化步骤注释与顺序保持：沿用 LAB5 的 7 步流程，但插入了文件复制步骤，并在步骤 5 的持锁区同时做 hash、pid 分配和父子链维护，减少竞态窗口。

完成以上修改之后，运行make grade，测试得分

![Grade](C:\Users\Tabito\Desktop\OS-Experiment\lab8\实验报告\Grade.png)

测试部分执行程序，结果均为Pass

![Pass](C:\Users\Tabito\Desktop\OS-Experiment\lab8\实验报告\Pass.png)

## 扩展练习 Challenge1：完成基于“UNIX的PIPE机制”的设计方案

> 如果要在ucore里加入UNIX的管道（Pipe）机制，至少需要定义哪些数据结构和接口？（接口给出语义即可，不必具体实现。数据结构的设计应当给出一个（或多个）具体的C语言struct定义。在网络上查找相关的Linux资料和实现，请在实验报告中给出设计实现”UNIX的PIPE机制“的概要设方案，你的设计应当体现出对可能出现的同步互斥问题的处理。）

- 核心数据结构（示例）
  - `struct pipe_ring`：环形缓冲区
    - `char *buf; size_t size; size_t rpos; size_t wpos;`
    - `size_t data;` // 已有字节数
  - `struct pipe`：
    - `struct pipe_ring ring;`
    - `int readers; int writers;` // 引用计数
    - `bool closed_read; bool closed_write;`
    - 同步原语：`struct semaphore empty, full; struct mutex lock;`
    - 等待队列：`wait_queue_t rq_readers; rq_writers;`
  - `struct file` 扩展（或重用现有 `files_struct` 与 `file`）：
    - `enum file_type { FT_REG, FT_DIR, FT_PIPE, ... };`
    - `void *private;` // 指向 `struct pipe *`
    - `ssize_t (*read)(struct file*, void *buf, size_t len);`
    - `ssize_t (*write)(struct file*, const void *buf, size_t len);`
    - `int (*close)(struct file*);`
    - `int flags;` // O_NONBLOCK 支持
    - `off_t off;` // 对 pipe 可忽略
  - `struct files_struct` 无需大改，pipe 由两个 `file*` 实例共享同一 `struct pipe*`。

- 关键接口（语义）
  1) `int sys_pipe(int fd[2]);`
     - 分配一个 `pipe` 对象；创建两份 `file` 结构：`fd[0]` 读端（只读），`fd[1]` 写端（只写）；引用计数 readers=1, writers=1。
  2) `ssize_t pipe_read(struct file *f, void *buf, size_t len);`
     - 阻塞语义：若 `ring.data == 0` 且 writers>0，则睡眠在 `rq_readers`。若 writers==0 则返回 0（EOF）。
     - 非阻塞：若空则返回 -EAGAIN。
     - 读取 min(len, ring.data) 字节，唤醒写者。
  3) `ssize_t pipe_write(struct file *f, const void *buf, size_t len);`
     - 若 readers==0，则返回 -EPIPE（并产生 SIGPIPE，在 uCore 可简化为错误码）。
     - 阻塞语义：当缓冲满 `ring.data == ring.size` 时睡眠在 `rq_writers`。
     - 非阻塞：满则返回 -EAGAIN。
     - 写入后唤醒读者。
  4) `int pipe_close(struct file *f);`
     - 若是读端，readers--；若为写端，writers--；当任一端计数归零时设 closed_* 并唤醒对端等待队列；当 readers==0 && writers==0 释放 pipe 缓冲与对象。
  5) 选配接口：
     - `int pipe_poll(struct file *f, struct poll_table *pt);` // 支持 select/poll
     - `int pipe_ioctl(...)` // 可选
     - `ssize_t pipe_splice(...)` // 可选零拷贝，非必需

- 同步/互斥策略
  - 使用 `mutex lock` 保护 ring 元数据和计数 readers/writers。
  - 读/写时的睡眠与唤醒：
    - 读方：空且有写者 → 睡眠；有数据 → 读并唤醒写者。
    - 写方：满且有读者 → 睡眠；无读者 → -EPIPE；有空间 → 写并唤醒读者。
  - 关闭时唤醒对端：关闭写端后唤醒读者，让其返回 0；关闭读端后唤醒写者，让其得到 -EPIPE。
  - O_NONBLOCK 通过 `file->flags` 判断，避免睡眠直接返回 -EAGAIN。
  - 避免死锁：持锁后再检查条件，若需睡眠则释放锁后进入等待队列，醒来再加锁检查（典型 “循环+条件变量/等待队列” 模式）。
  - 中断上下文不进入 pipe 阻塞路径；仅用户/进程上下文可睡眠。

- 内存与生命周期
  - `sys_pipe` 分配 `pipe` 对象与其 `buf`（固定 PGSIZE 或可调 `PIPE_BUF`）。
  - `pipe_close` 负责引用计数与最终释放。
  - 发生 fork 时，`files_struct` 的复制需增加 readers/writers 计数，遵循 CLONE_FILES 逻辑。

- 错误码与语义
  - 空读且写端关闭：返回 0（EOF）。
  - 空读且阻塞：睡眠；非阻塞：-EAGAIN。
  - 满写且读端关闭：-EPIPE。
  - 满写且阻塞：睡眠；非阻塞：-EAGAIN。
  - 非法操作（在读端写/在写端读）：-EINVAL。

## 扩展练习 Challenge2：完成基于“UNIX的软连接和硬连接机制”的设计方案

> 如果要在ucore里加入UNIX的软连接和硬连接机制，至少需要定义哪些数据结构和接口？（接口给出语义即可，不必具体实现。数据结构的设计应当给出一个（或多个）具体的C语言struct定义。在网络上查找相关的Linux资料和实现，请在实验报告中给出设计实现”UNIX的软连接和硬连接机制“的概要设方案，你的设计应当体现出对可能出现的同步互斥问题的处理。）

- 核心数据结构（示例）
  - `enum inode_type { I_REG, I_DIR, I_SYMLINK, ... };`
  - `struct inode` 扩展字段：
    - `uint32_t i_nlink;` // 硬链接计数
    - `enum inode_type i_type;`
    - `union { struct { char target[SYMLINK_MAX]; size_t len; } sl; /* 软链 */ };`
    - `struct super_block *sb; struct inode_ops *i_op; struct file_ops *f_op;`
    - `struct mutex i_lock;` // 保护元数据（nlink、size、symlink target 等）
  - `struct dentry`：
    - `char name[MAX_NAME]; struct inode *inode; struct dentry *parent;`
    - `int flags; // DENTRY_NEGATIVE 等`
    - `struct mutex d_lock;`
  - `inode_ops` 需新增钩子：
    - `int (*link)(struct dentry *old_d, struct inode *dir, struct dentry *new_d);`
    - `int (*unlink)(struct inode *dir, struct dentry *d);`
    - `int (*symlink)(struct inode *dir, struct dentry *d, const char *target);`
    - `int (*readlink)(struct dentry *d, char *buf, size_t buflen);`
    - `struct dentry *(*lookup)(struct inode *dir, const char *name);` // 已有，需支持返回 SYMLINK
  - `file_ops` 对 symlink 通常只需 `readlink`（不做常规读写执行）。

- 关键接口语义
  - `int sys_link(const char *oldpath, const char *newpath);`
    - 解析 oldpath，取得目标 inode；解析 newpath 父目录，创建目录项指向同一 inode；`i_nlink++`；目录或特殊文件需校验（禁止对目录建硬链，除非特权）。
  - `int sys_unlink(const char *path);`
    - 解析父目录 + 名字，删除目录项；`i_nlink--`；若降为 0 且无打开文件，则回收 inode/数据块；目录需拒绝（或用 rmdir）。
  - `int sys_symlink(const char *target, const char *linkpath);`
    - 在父目录创建新 inode，`i_type=I_SYMLINK`，把 target 文本写入 `inode->sl.target` 或数据块；`i_nlink=1`。
  - `ssize_t sys_readlink(const char *path, char *buf, size_t buflen);`
    - 不跟随最终一跳，直接读 symlink 的 target 文本。
  - `namei`/路径解析：
    - 默认跟随 symlink；支持 `O_NOFOLLOW`；限制最大跳数（如 40）防止循环；遇到 `I_SYMLINK` 时读取 target 重新解析。
  - 目录项更新：
    - `link/unlink/symlink` 需更新目录数据块与 dentry 缓存。

- 同步/互斥与一致性
  - `i_lock` 保护 inode 的 `i_nlink`、`i_type`、symlink target、size。
  - 目录级锁（如 `dir->i_lock` 或单独 `d_lock`）保护目录项增删，避免并发创建/删除同名。
  - 递归/双锁顺序：先锁父目录，再锁目标 inode，约定固定顺序防止死锁。
  - Path resolution 中遇到 symlink 读 target 前需持有 inode 读锁；释放后再做字符串替换与重新解析。
  - 硬链接：更新 `i_nlink` 必须和目录项写入同一临界区，失败需回滚计数。
  - 回收：`unlink` 后若 `i_nlink==0` 且无打开文件，触发 `truncate + free_blocks + iput`。

- 持久化/磁盘格式（简略）
  - 磁盘 inode 增加字段：`nlink`、`type`、`symlink_inline[SYMLINK_INLINE_MAX]` 或指向数据块存储目标路径。
  - 目录项格式无需变化：`name + inode_no`；symlink 与硬链接均是普通目录项指向 inode。

- 辅助/校验
  - 软链接最大长度限制 `SYMLINK_MAX`。
  - 循环检测：路径解析维护跳转计数，超过上限返回 `-ELOOP`。
  - 权限：创建硬链需对源和目标目录有合适权限；硬链到目录默认拒绝；`unlink` 对只读文件系统返回 `-EROFS`。

该设计在不实现具体代码的前提下，给出了需要的结构扩展、接口语义和并发控制思路，便于在 uCore 的 VFS/FS 层落地。